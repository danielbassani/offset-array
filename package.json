{
  "name": "offset-array",
  "version": "1.0.1",
  "description": "A utility to generate the offset from each number in the array from a given value, or the mean of the array. Will remove non-numeric types from the provided list.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "danielbassani",
  "license": "ISC",
  "keywords": ["array", "numbers", "offset", "mean", "average", "normalize"]
}
